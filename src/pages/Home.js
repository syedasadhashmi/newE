import Carousel from "../components/UI/Carousel";
import ProductCards from "../components/UI/ProductCards";

const Home = (props) => {
  return (
    <>
      <Carousel />
      <ProductCards />
    </>
  );
};
export default Home;
